{% extends "base.html.twig" %}

{% block body %}

    <section class="container">

        <h1 class="center">Ajouter une notice</h1>

        {{ form_start(bookForm)}}
        {{ include('AdminBundle:Book:_form.html.twig')}}
        <button class="btn black right" type="submit" formnovalidate >Créer</button>
        {{ form_end(bookForm)}}

    </section>

{% endblock %}